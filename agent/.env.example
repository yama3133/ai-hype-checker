AWS_REGION=us-east-1
# AgentCore Gateway (Web Searchコネクタ) のMCPエンドポイント。
# 未設定時は tools/web_search.py 内のデフォルトを使用。自分のGatewayを使う場合のみ設定
# WEB_SEARCH_GATEWAY_URL=https://<gateway-id>.gateway.bedrock-agentcore.us-east-1.amazonaws.com/mcp
